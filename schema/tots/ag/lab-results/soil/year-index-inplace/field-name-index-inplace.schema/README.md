# modus_soil_lab_results

Production Modus-oriented soil lab result leaf. Sample analyte columns are open (`additionalProperties: true`). UNITS and MODUSTESTID typed rows store per-column strings in `meta.samples`.
